import React from 'react';
import useLaptop from '../../Hooks/useLaptop';

const Inventory = () => {

    return (
        <div>
            {
            }
        </div>
    );
};

export default Inventory;